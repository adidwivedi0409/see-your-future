fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2
python-multipart>=0.0.9
python-dotenv>=1.0
httpx>=0.27
pypdf>=4.0
icalendar>=5.0
pytest>=8.0
# --- Optional integrations (enabled via env vars; all imports are guarded, demo mode needs none of them) ---
# strands-agents[anthropic]   # FutureNavigatorAgent real LLM path (needs ANTHROPIC_API_KEY, DEMO_MODE=false)
# cognee                       # Personal brain knowledge graph (needs COGNEE_API_KEY or local cognee install)
# brightdata-sdk               # Live opportunity discovery (needs BRIGHT_DATA_API_KEY); REST path uses httpx only
